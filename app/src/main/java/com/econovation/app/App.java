/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.econovation.app;

import com.econovation.list.LinkedList;

import static com.econovation.utilities.StringUtils.join;
import static com.econovation.utilities.StringUtils.split;
import static com.econovation.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
